window.WIN10BET_CONFIG = {
  // Leave empty when the HTML and Node server run on the same domain.
  // Example for GitHub Pages front + Render/Railway API:
  // apiBase: "https://your-win10bet-api.onrender.com"
  apiBase: ""
};
